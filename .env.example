# Path to .beads database (auto-discovered if not set)
# For SQLite: path to .db file (e.g. /path/to/.beads/issues.db)
# For Dolt: path to dolt repo directory (e.g. /path/to/dolt-repo)
BEADS_DB=

# Server port
PORT=3333

# Dolt connection settings (used when BEADS_DB points to a Dolt repo)
DOLT_HOST=127.0.0.1
DOLT_PORT=3306
DOLT_USER=root
DOLT_PASSWORD=
DOLT_DATABASE=beads
